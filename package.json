{
  "name": "candicar",
  "version": "0.1.0",
  "private": true,
  "description": "CandiCar — mise en relation moniteurs auto-ecole / candidats libres",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
